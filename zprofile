# ~/.zprofile
# Sourced for login shells only — once when your session starts.
# Slow or one-time setup lives here: Homebrew, PATH, session-level env.

[[ -f "$ZDOTDIR/path.zsh" ]] && source "$ZDOTDIR/path.zsh"
